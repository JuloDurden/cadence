VITE_API_URL=http://localhost:3001
# Démo publique v1 (2026-09-11) : "true" uniquement sur le déploiement de démo Vercel, masque
# "Créer un compte" sur l'écran de connexion (voir LoginPage.tsx). Absente ou "false" sur
# l'instance réelle.
VITE_DISABLE_SIGNUP=false
