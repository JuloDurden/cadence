{
  "name": "cadence-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Serveur MCP Cadence : expose le Backlog, les sprints, l'equipe et les clients a Claude, en lecture seule, avec les memes droits que le jeton utilise.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.3"
  }
}
