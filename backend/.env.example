DATABASE_URL="postgresql://cadence:cadence@localhost:5432/cadence"
JWT_SECRET="remplacer-par-un-secret-fort-en-production"
PORT=3001
FRONTEND_URL="http://localhost:5173"
# Démo publique v1 (2026-09-11) : "true" uniquement sur le déploiement de démo Railway, bloque
# POST /api/auth/signup (voir routes/auth.ts). Absente ou "false" sur l'instance réelle.
DISABLE_SIGNUP="false"
